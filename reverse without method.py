def func_name(reverse_list):
# reverse_list = ["Z", "A", "A", "B", "E", "M", "A", "R", "D"]
    x=[]
    i=0
    while i<len(reverse_list):
        b=(reverse_list[::-1])
        i+=1
    print(b)
reverse_list = ["Z", "A", "A", "B", "E", "M", "A", "R", "D"]
func_name(reverse_list)

